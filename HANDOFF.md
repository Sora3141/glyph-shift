# Glyph Shift ソルバー 引き継ぎメモ（2026-09-23）

新しいチャットで開発を続けるための要約。リポジトリは `/Users/kases/dev/square-game`（GitHub: Sora3141/glyph-shift）。
遊び方と画面の説明は `README.md` にある。ここではソルバー（`solver.js`）の状態だけを書く。
背景の詳しい経緯は `~/.claude/projects/-Users-kases-dev/memory/square-game-solver.md` にもある。

## 作業前に必ず読むこと

- **このリポジトリは複数のチャットが同時に触ることがある。** 実際、2026-09-23 に画面まわりのチャットと
  並行して作業し、その間に HEAD が 6 回進んだ。作業前に `git log --oneline -3` と
  `git status --short` を見て、自分の変更が残っているか確かめる。
- **能力（ブロック）は 13 種ある。** 2026-09-23 に 3 種増えた（`sqUD` 四角のすみを上下、
  `sqLR` 四角のすみを左右、`none` 何も起きないマス）。**それ以前に取った盤ごとの手数はすべて使えない。**
  `newPuzzle` の出す盤が変わるため。baseline を作るときは必ず「比べたい版の solver.js + いまの script.js」で組む。
- **計測は 1 プロセス 1 盤で回す。** 複数盤を同じプロセスで回すと結果が大きく揺れ、同じ盤が
  421 手と 363 手の間で動いたり、解けた盤が逆手順に落ちたりする。1 プロセス 1 盤なら完全に再現する。
  以前のメモにあった「揺れるので数回繰り返す」は、この原因だった。

## いまの状態

- テストは `node tools/runtests.js` で 40 本すべて通る。ただし**これはゲームと画面のテストで、
  ソルバーの中身は覆っていない**（solver.js の `keyOf` をわざと壊しても 40 本通った）。
  ソルバーの検証は `tools/bench.js` の検算（`bad=0`）が担当する。
- 未コミットのものは無い。

## ゲームの前提（ソルバーに関わる部分）

- 盤は W×H（3〜10）。各マスにタイル。タイルは 13 種の能力のどれかを持ち、押すと周囲のマスを動かす。
  押したタイル自身は動かない。`none` だけは押しても何も起きない（が、まわりからは動かされる）。
- 能力は座標ではなくタイルに付く。同じ能力のタイルは互換なので、状態は「絵柄（能力）の並び」だけでよい。
- 効果が盤の外に出る位置では押せない。位置で変わるのは押せるかどうかだけで、効果の形は平行移動で不変。
- どの能力も効果は押す場所の周囲 8 マスに収まる。
- 生成は完成状態から Metropolis 補正つきランダムウォークで混ぜる。混ぜた道筋の逆順が「逆手順」
  （必ず解ける保険、1 万〜2 万手）。

## solver.js の構成

`solverModule()` は画面に依存しない純粋な関数。`script.js` がこの関数を文字列化して Blob から
Web Worker を作る（file:// でも動く）。Worker が無い環境では同期で短い予算。
問題の記述 `P = { W, H, N: W, SIZE, cyc, ord }`。`cyc[a][i]` は能力 a をマス i で使ったときの
サイクル（使えなければ null）。`widthOf(P)/heightOf(P)` で読む。

`solvePuzzle(P, start, goal, budgetMs, fallbackPlan, report)` の流れ（候補を集めて最短を返す）:

1. **目標側 BFS 表** `buildGoalTable` … 以降の方法が「表に当たったら最短で終わる」ために使う。
2. **双方向 BFS** `bidirectional` … 厳密最短。64 マス未満の盤だけ。
3. **ビーム** `bidirectionalBeam` … 評価は色ごとの距離の合計（類＝偶奇を考慮）。色数 4 以下なら積み上げより先。
4. **積み上げ** `constructive` … 1 マスずつ確定していく人間流。
   - 走査順 `scanOrder`: `hole` → `spiral` → `rows`。`band` は却下済みの死にコード。**復活させないこと**。
   - 終盤の穴 3×3: `pickHole` が位置を選ぶ。第 1 段階は枠の群（`frameGroup`、厳密）、
     届かない候補だけ第 2 段階の楽観的な見積もり。
5. **逆手順** … 保険。
6. **経路短縮** `shortcutRepeat` … 全候補にかけ、いちばん短いものを返す。

### 可換な 2 手の枝刈り（2026-09-23 追加）

押す場所 i と j の効果範囲が重ならず、互いの押す場所も相手の効果範囲に入らなければ、その 2 手は可換。
「直前に押したマスと可換なマスは、番号が大きい側だけ押してよい」という規則を
`bidirectional` と `bidirectionalBeam` に入れてある（`skipByOrder` / `pressesCommute`）。
隣り合う可換な 2 手は入れ替えても同じ状態・同じ長さなので、最短は取りこぼさない。

盤上の押し場所の組の 82〜88% が可換で、深さ 2 の手順の 45〜64% が既に見た状態へ戻っていた。
双方向 BFS は 6〜18% 速くなり手数は不変。ビームは 36 盤で 同じ 22 / 短く 8（計 -243 手）/ 長く 6（計 +23 手）。

**判定の作り方に注意。** 「離れていれば可換」という近道だけでは逆効果になる（双方向 BFS が 8〜21% 遅くなった）。
この関数が走るのは 64 マス未満の盤で、5×5 では全マスが距離 2 以内にあり近道が一度も効かないため。
いまは能力ごとの効果範囲を平らな配列に畳み、印つけで重なりを見ている。

## 到達点（予算 5〜7 秒、能力 13 種、1 プロセス 1 盤）

| 盤 | 結果 |
|---|---|
| 4×4, 5×5（3〜4 色） | 最短保証 |
| 6×6 4色 / 8×8 3色 | 約 56 手 / 約 48 手（ビーム） |
| 10×10 5色 seed 2000〜2003 | 3/4（2000:218, 2001:290, 2003:317、2002 は逆手順） |
| 9×9 8色 seed 2000〜2007 | 6/8（2001 と 2004 が逆手順） |
| 10×10 8色 seed 5000〜5005 | 4/6（5000 と 5002 が逆手順） |
| 10×10 10色 seed 3000〜3003 | 4/4、約 410〜550 手 |
| 6×10 5色 / 10×7 7色 seed 5007 | 56 手 / 259 手 |

## 計測のしかた

```bash
node tools/runtests.js              # ゲームと画面のテスト 40 本
node tools/bench.js <幅>x<高さ> <色数> <盤面数> [予算ms] [最初のSEED]
node tools/bench.js 10 5 1 5000 2000    # 1 プロセス 1 盤で回すこと
```

手順の合法性と終点は bench が検算する（`bad=0` を確認）。

## 残っている課題

### 1. 終盤で「離れた 2 マスが入れ替わっている」形（最大の未解決）

解けない盤（10×10 8色 s5000・s5002、9×9 8色 s2001・s2004）は、**予算を十分に与えると
積み上げが「残り 2 マス」まで進む**。パイプラインでは予算が足りず 9 マス以上残って見えるだけ。

その 2 マスは互いの絵柄を持っていて、しかも離れている（s5002 で距離 7、s2004 で距離 5）。
2 マスの入れ替えは奇置換 1 つぶんで、確定マスに触らないかぎり作れない
（マクロが作るのは主に 3 サイクル＝偶置換）。その盤面からビームを打っても
15 秒かけて届かない。評価が「色ごとの最短距離の合計」なので、遠くへ運ぶために
一度大きく悪化させる道を選べないため。

穴の群も貧しい。どの候補も枠で全配置に届かず（`frameFull` が 0 個）、最良でも 0.5〜2.1%。
比較: 解ける 10×10 8色 s5001 は `frameFull` が 8 個・100%。

**残っている壁は終盤の直し方ではなく、`constructive` が終盤に届く速さ。**
いまは残り数マスに届くまで 5〜8 秒かかる。パイプラインの予算は合計 7 秒で、最初の穴の
試行に渡るのはその 6 割ほど。ここが 2〜3 秒まで縮めば、下の枝の仕組みがそのまま効くはず。

**試して効かなかったこと（同じ道を繰り返さないこと）**

- **枠を広げる**（`FRAME_OFFSETS` を ±2 → ±4 → ±6、9 → 25 → 49 か所）。
  8 盤で `full` も最良%も**完全に同一**。費用だけ 10〜70 倍。
  理由: 距離 4 以上の押し手の効果範囲は穴に届かないので、穴 8 マスへの作用がまったく増えない。
- **狙って軌道に入れる回数を増やす**（`retry` の `attempt < 6` を 16、40 に）。6 盤で結果が変わらない。
- **崩す深さを増やす**（`depth` の上限 3 を 6、10、16 に）。同じく変わらない。
  上の「回数を増やす」が効かなかったのは深さが 3 で頭打ちだったからだが、深さを上げても効かない。
- **確定マスを崩して直す（`repair`）**。枝 `repair-experiment` に置いてある。
  単体では効き、`constructive` に直接 30 秒与えると s5000 が 484 手、s5002 が 318 手で解けた。
  だがパイプラインでは予算が足りず出番が来ない。予算 25 秒で回しても 4 盤とも解けず、
  s2004 は repair なしなら 318 手で解けるのに repair ありだと逆手順に落ちる（時間を食うため）。
  枝のコミットに、その中で試してやめた 2 つのやり方（絵柄で選んで一括開放／欠けを 1 歩ずつ歩かせる）も
  なぜ駄目かと一緒に書いてある。

### 2. 多色盤で必ず失敗するビームが予算の 4 割を食う

10×10 8色のプロファイルで、最後の `runBeam` が総時間の 42% を使って毎回 null を返していた。
色数と盤の大きさで条件を付けて切れば、その分を積み上げに回せる。未着手。

### 3. `pickHole` の第 2 段階（楽観的な見積もり）

枠で届く穴が 3 個そろえば走らないようにしたので、豊かな盤では消えた。
貧しい盤では全候補で走る。逆引き索引で置き換えられないかを試したが、
本物との順位相関が ρ = 0.21〜0.58 しかなく使えなかった（台が 3×3 に収まる部品しか数えられず、
本物が使う「外を一時的に崩して戻す」マクロを取りこぼすため）。

### 4. 6×6 以上の最短保証は原理的に無理（状態数）

「短い一例」を目指す方針は変えない。

## 補助スクリプト

- `tools/bench.js` … ソルバーの計測と検算。
- `tools/runtests.js` … ゲームと画面のテスト 40 本。
- `tools/swunit.js` … Service Worker のテスト（runtests から別プロセスで呼ばれる）。
- `tools/invariance.js` … 能力の効果が平行移動で不変であることの実測。
